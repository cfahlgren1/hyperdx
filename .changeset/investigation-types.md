---
"@hyperdx/common-utils": patch
---

Add `investigation` to the alert history schema and the
`AlertInvestigationItem` / `AlertInvestigationsApiResponse` types backing the
new alert investigations endpoint.
