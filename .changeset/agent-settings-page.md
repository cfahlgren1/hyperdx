---
'@hyperdx/api': minor
'@hyperdx/app': minor
'@hyperdx/common-utils': patch
---

Add an AI Agent settings tab: the investigations switch, CRUD for the agent's durable memories, and team-authored agent instructions injected into every investigation prompt (editable only by users, never by the agent).
